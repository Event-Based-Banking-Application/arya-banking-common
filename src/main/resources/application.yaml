spring:
  application:
    name: arya-banking-common

  data:
    mongodb:
      uri: mongodb+srv://admin:${MONGO.PASSWORD}@bankingcluster.ayhvgpk.mongodb.net/metadata?retryWrites=true&w=majority&appName=bankingCluster

  kafka:
    bootstrap-servers: localhost:9092
    properties:
      schema.registry.url: http://localhost:8081

app:
  metadata:
    versioning:
      enabled: true